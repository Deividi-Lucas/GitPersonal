import styled from 'styled-components'
import { color } from '../../utils/color'

export const Main = styled.div`
  background: ${color.roxoDark};
  display: flex;
  flex-direction: column;
  align-items: center;
  height: 80vh;
  gap: 0.5rem;
`
